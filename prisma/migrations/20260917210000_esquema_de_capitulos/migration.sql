-- Los capítulos del documento cuando el reglamento de la facultad los numera de
-- otra forma que el método. Nulo = la estructura del catálogo, la de siempre.

-- AlterTable
ALTER TABLE `projects` ADD COLUMN `esquema` JSON NULL;
